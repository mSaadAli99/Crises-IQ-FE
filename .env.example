# Copy to .env for local development (not committed).
# On Vercel: add the same variable in Project → Settings → Environment Variables.

# Railway backend public URL (no trailing slash)
EXPO_PUBLIC_API_URL=https://your-service.up.railway.app
